# Updating Flutter
You can update the flutter version for this project by running `fvm use -p stable`. If `-p` is omitted, the version will track the stable branch, including updates; ensure `-p` is not omitted.
